<view class="rank">
  <view class="rank_wapper">
    <block wx:for="{{songlist}}" wx:key="id" bindtap="hanldeClick" data-song="{{item}}">
      <s-list item="{{item}}" index="{{index}}" class="song_list" righticon="{{false}}" lefticon="{{true}}"></s-list>
    </block>
  </view>
</view>
